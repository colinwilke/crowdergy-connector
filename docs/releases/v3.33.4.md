## v3.33.4 — Coordinator disentanglement, Phase C (#21)

Internal refactor only — **no user-facing behaviour change**, no config-flow
or onboarding change, no re-provisioning needed.

### Changed

- **`TelemetryReaderMixin` extraction (#21 Phase C):** the 13 per-tick
  read/compose/decide helpers (`_get_state`, `_prefetch_device_states`,
  `_read_*`, `_compose_extra`, `_payload_hash`, `_should_send`,
  `_normalised_vehicle_status`) and their send-decision / extra-field
  constants moved out of `coordinator.py` into a new `telemetry_reader.py`
  as `TelemetryReaderMixin` — mirroring the `CommandDispatcherMixin` split.
  `coordinator.py` shrinks ~471 lines. Behaviour-preserving; the constants
  are re-exported from `coordinator` so existing module-attribute imports
  keep resolving. The auth cluster, the consent gates and `_async_update_data`
  stay in `coordinator.py`.

No HA-version bump, no entity/preset changes → no new Box-vendor matrix row.
