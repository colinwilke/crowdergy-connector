## v3.31.1

### Interne Aufräumarbeit — God-File-Split (#50)

Rein interne Code-Strukturierung, **keine funktionale Änderung** und keine
Re-Provisionierung nötig:

- `config_flow.py` (3847 Zeilen) → Schema-Builder, Preset-Resolver und
  Mapping-/Device-Record-Helfer in eigene Module ausgelagert
  (`config_flow_schemas.py`, `config_flow_presets.py`,
  `config_flow_mapping.py`). Die HA-`ConfigFlow`/`OptionsFlow`-Klassen
  bleiben in `config_flow.py`.
- `coordinator.py` (2777 Zeilen) → die SSE-Befehls-Anwendung und die
  Hold-Enforcement (`_apply_*`, `_handle_ws_message`, Hold-Loops) in einen
  `CommandDispatcherMixin` (`command_dispatcher.py`) verschoben, den der
  Coordinator erbt.

Verhalten, Services, Entry-Schema und Entity-Mapping sind unverändert.
